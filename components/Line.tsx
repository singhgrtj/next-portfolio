import React from 'react';

interface Props {
}

const Line: React.FC<Props> = (props) => {
    return (
        <div className="bg-primary w-full border-2 border-primary mt-2" />
    )
}

export default Line;
